/**
 * Twitter/X card. Same artwork as the Open Graph image — there is no reason for the two
 * to differ, and duplicating the JSX would let them drift apart.
 */
export { default, alt, size, contentType } from "./opengraph-image";
